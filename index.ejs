<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-100 flex items-center justify-center h-screen">
    <div class="max-w-xl w-full p-8 bg-white shadow-lg rounded-lg">
        <h1 class="text-3xl font-bold text-center mb-6 text-blue-600">Chat with our Bot</h1>
        
        <!-- Form to enter message -->
        <form action="/sendMessage" method="POST" class="flex flex-col items-center space-y-4">
            <input type="text" name="userInput" placeholder="Type your message..." required class="w-full p-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-400 shadow">
            <button type="submit" class="w-full bg-blue-500 text-white p-3 rounded-lg shadow-lg hover:bg-blue-600 transition duration-200">Send Message</button>
        </form>

        <!-- Chatbot Response Box -->
        <div class="mt-8 p-4 border rounded-lg bg-gray-50 shadow-lg">
            <h1 class="text-xl text-gray-700 font-medium">
                <% if (locals.content) { %>
                <%= content %>
                <% } else { %>
                Waiting for your response...
                <% } %>
            </h1>
        </div>
    </div>
</body>
</html>